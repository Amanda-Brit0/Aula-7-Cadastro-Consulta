<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirmar Exclusão</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container text-center">
        <h1>Deseja excluir este registro?</h1>
        <p>Nome: {{post.nome}}</p>
        <p>Telefone: {{post.telefone}}</p>
        <p>Origem: {{post.origem}}</p>
        <p>Data do Contato: {{post.data_contato}}</p>
        <p>Observação: {{post.observacao}}</p>


        <form action="/confirmar-exclusao" method="POST">
            <input type="hidden" name="id" value="{{post.id}}" />
            <button type="submit" class="btn btn-danger">Confirmar Exclusão</button>
        </form>
        <br>
        <a href="/consultar" class="btn btn-secondary">Cancelar</a>
    </div>
</body>
</html>